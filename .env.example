# OpenAI API key used by Stage 3 (RAG + clinical reasoning)
OPENAI_API_KEY=your-openai-api-key-here

# Optional override for model directory. If set, model filenames from config are resolved from here.
# THESIS_MODELS_DIR=C:/path/to/models
